import { IFeature } from "../type";

export const community: IFeature = {
  code: {
    description: "Code",
    enabled: true,
    pull: {
      description: "Pull Code",
      enabled: true,
    },
  },
};
