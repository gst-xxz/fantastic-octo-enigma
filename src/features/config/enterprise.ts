import { IFeature } from "../type";

export const enterprise: IFeature = {
  code: {
    description: "Code",
    enabled: true,
    pull: {
      description: "Pull Code",
      enabled: true,
    },
  },
};
